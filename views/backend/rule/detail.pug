extends ../../_templates/main-layout.pug

block content
    div.box
        input(type="hidden", name="id", value=(rule != undefined ? `${rule.id}` : ''), disabled=editMode ? null : '')
        input(type="text", name="title", placeholder="Titel", value=(rule != undefined ? `${rule.title}` : ''), disabled=editMode ? null : '')